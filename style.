style.css
/* --- Programs Page Specific Styles (Tables) --- */

.section-padding {
    padding: 4rem 0;
}

.stats-table table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 2rem;
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
}

.stats-table th, .stats-table td {
    padding: 15px;
    text-align: left;
    border-bottom: 1px solid #ddd;
}

.stats-table th {
    background-color: var(--primary-color);
    color: white;
    font-weight: 700;
}

.stats-table tbody tr:nth-child(even) {
    background-color: var(--light-bg);
}

.stats-table tbody tr:hover {
    background-color: #e0e0e0;
}

/* Ensure mobile layout for the table is readable */
@media (max-width: 600px) {
    .stats-table table, .stats-table thead, .stats-table tbody, .stats-table th, .stats-table td, .stats-table tr { 
        display: block; 
    }
    
    .stats-table thead tr { 
        position: absolute;
        top: -9999px;
        left: -9999px;
    }
    
    .stats-table td { 
        border: none;
        border-bottom: 1px solid #eee; 
        position: relative;
        padding-left: 50%; 
        text-align: right;
    }
    
    .stats-table td:before { 
        position: absolute;
        top: 6px;
        left: 6px;
        width: 45%; 
        padding-right: 10px; 
        white-space: nowrap;
        text-align: left;
        font-weight: bold;
        color: var(--primary-color);
    }
    
    /* Labeling the data on mobile */
    .stats-table td:nth-of-type(1):before { content: "KPI:"; }
    .stats-table td:nth-of-type(2):before { content: "Target / Baseline:"; }
    .stats-table td:nth-of-type(3):before { content: "Benefit Realized:"; }
}
